# Payload Admin (apps/admin) — lokale/CI-Entwicklung
# Prod: eigene Postgres, starkes PAYLOAD_SECRET, hinter Basic-Auth/IP-Filter (eigene Subdomain).
DATABASE_URI=postgresql://postgres@127.0.0.1:5433/residentflow
PAYLOAD_SECRET=change-me-in-prod
